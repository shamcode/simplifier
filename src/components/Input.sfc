<template>
    <input
        id={{id}}
        placeholder={{placeholder}}
        tabindex="{{tabindex}}"
        name={{name}}
    />
</template>

<script>
    function Input( options ) {
        options( {
            [ ref( 'placeholder' ) ]: '',
            [ ref( 'tabindex' ) ]: 1
        } );
    }

    export default Component( Template, Input );
</script>
